import { Meta, Canvas, Story, Controls } from "@storybook/addon-docs";
import { Select } from "./Select.tsx";
import * as SelectStories from "./Select.stories.tsx";

<Meta of={SelectStories} component={Select} />

# Select

This is a select component used for various purposes in our application.

### Default

<Canvas of={SelectStories.Default} />

## Props

<Controls of={SelectStories.Default} />
